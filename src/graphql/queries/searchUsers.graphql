# query searchUsers($arg: String!, $courseId: String!) {
#   searchUsers(arg: $arg, courseId: $courseId) {
#     id
#     names
#     surnames
#     email
#   }
# }
query searchUsers($courseId: String!) {
  searchUsers(courseId: $courseId) {
    id
    names
    surnames
    email
  }
}
