query courses($page: Int!, $per_page: Int) {
  courses(page: $page, per_page: $per_page) {
    prev
    totalPages
    data {
      id
      totalUsers
      statusText
      hasTestText
      courseDetail {
        id
        name
        coverPhoto
      }
    }
  }
}
