query courses($args: PaginatedArgs!) {
  courses(args: $args) {
    prev
    totalPages
    data {
      id
      totalUsers
      status
      statusAction
      statusText
      hasTestText
      courseDetail {
        id
        name
        coverPhoto
      }
    }
  }
}
